#!/bin/bash
mv * /etc/update-motd.d/

chmod -x /etc/update-motd.d/*

chmod +x /etc/update-motd.d/00-header
chmod +x /etc/update-motd.d/05-logo
chmod +x /etc/update-motd.d/10-uptime
chmod +x /etc/update-motd.d/18-mounts
chmod +x /etc/update-motd.d/19-temps
chmod +x /etc/update-motd.d/20-services
chmod +x /etc/update-motd.d/22-pm2
chmod +x /etc/update-motd.d/90-updates-available
chmod +x /etc/update-motd.d/91-release-upgrade
chmod +x /etc/update-motd.d/92-unattended-upgrades
chmod +x /etc/update-motd.d/95-hwe-eol
chmod +x /etc/update-motd.d/98-fsck-at-reboot
chmod +x /etc/update-motd.d/98-reboot-required